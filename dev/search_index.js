var documenterSearchIndex = {"docs":
[{"category":"section","location":"developer_api/#Developer-API","page":"Developer API","text":"The names on this page are versioned extension points for packages that implement automatic-differentiation backends or PreallocationTools extensions. They are not intended for ordinary user code. Use the documented cache constructors and get_tmp unless you own such an extension.","title":"Developer API"},{"category":"function","location":"developer_api/#PreallocationTools.dualarraycreator","page":"Developer API","text":"dualarraycreator(u::AbstractArray, size, ::Type{Val{N}})\n\nDeveloper extension hook that constructs the dual-number workspace for FixedSizeDiffCache.\n\nThis is a PreallocationTools implementation API, not a stable end-user customization point. Extensions may add a method only when they own the dual-number type used in the returned storage.\n\nArguments\n\nu: primal cache prototype whose array representation must be preserved.\nsize: dimensions of the workspace to allocate.\nN: dual chunk size encoded by Val.\n\nReturn\n\nReturn an array with the requested dimensions that can store dual values with chunk size N. It must preserve the representation required by u.\n\n\n\n\n\n","title":"PreallocationTools.dualarraycreator"},{"category":"function","location":"developer_api/#PreallocationTools.forwarddiff_compat_chunk_size","page":"Developer API","text":"forwarddiff_compat_chunk_size(n::Integer)\n\nDeveloper extension hook returning the default dual chunk size for a cache with n primal elements.\n\nThis hook is used by DiffCache and FixedSizeDiffCache when their callers omit N. Extensions that own an AD backend may specialize it; the returned value must be a nonnegative integer accepted by that backend.\n\n\n\n\n\n","title":"PreallocationTools.forwarddiff_compat_chunk_size"},{"category":"function","location":"developer_api/#PreallocationTools.chunksize","page":"Developer API","text":"chunksize(::Type{T})\n\nDeveloper extension hook returning the AD chunk size encoded by scalar type T, or 0 when T carries no chunk-size information.\n\nExtensions may specialize this only for scalar types they own. The result is used to determine whether a fixed-size cache can reuse its dual workspace.\n\n\n\n\n\n","title":"PreallocationTools.chunksize"},{"category":"function","location":"developer_api/#PreallocationTools._restructure","page":"Developer API","text":"_restructure(normal_cache::AbstractArray, duals)\n\nDeveloper extension helper that gives flat dual storage the representation and shape of normal_cache.\n\nThis is an implementation API for PreallocationTools extensions, not an end-user customization point. duals must contain enough elements for normal_cache; the returned array must have size(normal_cache) and represent the same dual values. The default uses reshape for ordinary arrays and ArrayInterface.restructure for custom array representations.\n\nArguments\n\nnormal_cache: primal workspace whose representation is to be preserved.\nduals: flat dual-number storage to reshape or restructure.\n\n\n\n\n\n","title":"PreallocationTools._restructure"},{"category":"function","location":"developer_api/#PreallocationTools.enlargediffcache!","page":"Developer API","text":"enlargediffcache!(dc::DiffCache, nelem::Integer)\n\nDeveloper extension helper that enlarges dc.dual_du to hold nelem elements.\n\nThis is an implementation API for AD extensions. Call it only when an extension has established that its requested dual representation needs more storage than the cache currently owns. It emits the cache's one-time resize warning when dc.warn_on_resize is enabled.\n\nArguments\n\ndc: cache whose dual workspace is to be enlarged.\nnelem: required number of elements in dc.dual_du; it must be nonnegative.\n\nReturn\n\nReturns the resized dual workspace.\n\n\n\n\n\n","title":"PreallocationTools.enlargediffcache!"},{"category":"section","location":"preallocationtools/#API","page":"API","text":"","title":"API"},{"category":"type","location":"preallocationtools/#PreallocationTools.DiffCache","page":"API","text":"DiffCache(u::AbstractArray, N::Int = forwarddiff_compat_chunk_size(length(u)); levels::Int = 1, warn_on_resize::Bool = true)\nDiffCache(u::AbstractArray, N::AbstractArray{<:Int}; warn_on_resize::Bool = true)\n\nBuild a cache with storage for both the element type of u and the corresponding forward-mode automatic differentiation dual type.\n\nUse get_tmp(cache, u) to retrieve storage matching the element type of u. The levels keyword or vector-valued N supports nested automatic differentiation. Set warn_on_resize = false to suppress the warning emitted when get_tmp enlarges the dual cache, which can be useful when adaptive algorithms are expected to resize the cache.\n\nDiffCache also supports sparsity detection via SparseConnectivityTracer.jl.\n\nArguments\n\nu: prototype array whose primal storage is cached.\nN: ForwardDiff chunk size, or one chunk size per nested AD level.\n\nKeyword Arguments\n\nlevels: number of nested forward-mode AD levels when N is scalar.\nwarn_on_resize: whether to emit a one-time warning if dual storage grows.\n\nFields\n\ndu: primal workspace.\ndual_du: dual-number workspace, enlarged on demand when necessary.\nany_du: reusable temporary storage for nested dual reconstruction.\nwarn_on_resize: controls the resize warning policy.\n\nExamples\n\ncache = DiffCache(zeros(3), 2)\nworkspace = get_tmp(cache, zeros(3))\n\n\n\n\n\n","title":"PreallocationTools.DiffCache"},{"category":"type","location":"preallocationtools/#PreallocationTools.FixedSizeDiffCache","page":"API","text":"FixedSizeDiffCache(u::AbstractArray, N = Val{forwarddiff_compat_chunk_size(length(u))})\nFixedSizeDiffCache(u::AbstractArray, N::Integer)\n\nBuild a fixed-size cache with storage for both the element type of u and the corresponding forward-mode automatic differentiation dual type.\n\nUse get_tmp(cache, u) to retrieve the cache matching the element type of u. FixedSizeDiffCache is most useful when the dual chunk size is known in advance and the cache size does not need to grow during differentiation.\n\nArguments\n\nu: prototype array whose shape and primal element type determine the cache.\nN: ForwardDiff chunk size. Pass Val{N} to encode it in the cache type, or an integer as a convenience constructor.\n\nFields\n\ndu: primal workspace with the shape and storage type of u.\ndual_du: workspace for dual-number evaluations.\nany_du: reusable temporary storage for nested dual reconstruction.\n\nExamples\n\ncache = FixedSizeDiffCache(zeros(3), 2)\nworkspace = get_tmp(cache, zeros(3))\n\n\n\n\n\n","title":"PreallocationTools.FixedSizeDiffCache"},{"category":"type","location":"preallocationtools/#PreallocationTools.GeneralLazyBufferCache","page":"API","text":"b = GeneralLazyBufferCache(f=identity)\n\nA lazily allocated cache keyed by the concrete type of its input. Given u, b[u] or get_tmp(b, u) calls f(u) on the first lookup for typeof(u) and returns that same cached object for later lookups of the type.\n\nArguments\n\nf: function used to construct a cache object from the first input of each concrete type. The default is identity.\n\nFields\n\nbufs: map from concrete input type to the reusable object produced by f.\nf: cache-construction function.\n\nLimitation\n\nThe result is not type-inferred because the backing map has Any values. Use a function barrier around the lookup when inference matters.\n\nExamples\n\njulia> using PreallocationTools\n\njulia> cache = GeneralLazyBufferCache(T -> Vector{T}(undef, 2));\n\njulia> get_tmp(cache, Float64) === get_tmp(cache, Float64)\ntrue\n\n\n\n\n\n","title":"PreallocationTools.GeneralLazyBufferCache"},{"category":"type","location":"preallocationtools/#PreallocationTools.LazyBufferCache","page":"API","text":"b = LazyBufferCache(f = identity; initializer! = identity)\n\nA lazily allocated buffer cache. Given an array u, b[u] or get_tmp(b, u) returns an array with a shape chosen from size(u). The cache allocates that buffer on its first matching lookup and returns the same object on later lookups.\n\nArguments\n\nf: maps size(u) to the requested buffer dimensions. The default, identity, preserves the input shape.\n\nKeyword Arguments\n\ninitializer!: function applied once to each newly allocated buffer. The default leaves the buffer uninitialized; use, for example, buf -> fill!(buf, 0.0) when callers require initialized scratch storage.\n\nFields\n\nbufs: cache from prototype array type and requested dimensions to a reusable buffer.\nsizemap: shape-mapping function supplied as f.\ninitializer!: initialization function applied to newly allocated buffers.\n\nPass an explicit shape as b[u, s] or get_tmp(b, u, s) to override f for one cache entry. Scratch buffers are shared for matching keys, so callers must overwrite them before reading.\n\nExamples\n\njulia> using PreallocationTools\n\njulia> cache = LazyBufferCache();\n\njulia> size(get_tmp(cache, zeros(2), (3,)))\n(3,)\n\n\n\n\n\n","title":"PreallocationTools.LazyBufferCache"},{"category":"method","location":"preallocationtools/#Base.fill!-Tuple{DiffCache, Any}","page":"API","text":"fill!(dc::DiffCache, val)\n\nFill all allocated buffers in the DiffCache with the given value.\n\n\n\n\n\n","title":"Base.fill!"},{"category":"method","location":"preallocationtools/#Base.fill!-Tuple{FixedSizeDiffCache, Any}","page":"API","text":"fill!(dc::FixedSizeDiffCache, val)\n\nFill all allocated buffers in the FixedSizeDiffCache with the given value.\n\n\n\n\n\n","title":"Base.fill!"},{"category":"method","location":"preallocationtools/#Base.fill!-Tuple{GeneralLazyBufferCache, Any}","page":"API","text":"fill!(glbc::GeneralLazyBufferCache, val)\n\nFill all allocated buffers in the GeneralLazyBufferCache with the given value.\n\n\n\n\n\n","title":"Base.fill!"},{"category":"method","location":"preallocationtools/#Base.fill!-Tuple{LazyBufferCache, Any}","page":"API","text":"fill!(lbc::LazyBufferCache, val)\n\nFill all allocated buffers in the LazyBufferCache with the given value.\n\n\n\n\n\n","title":"Base.fill!"},{"category":"method","location":"preallocationtools/#Base.reshape-Tuple{DiffCache, Tuple{Vararg{Integer}}}","page":"API","text":"reshape(dc::DiffCache, dims...)\nreshape(dc::DiffCache, dims)\n\nReturn a DiffCache whose normal cache has shape dims.\n\nThis is useful for vector-backed caches that need to be resized. Resize the backing DiffCache with resize!, then call reshape again with the updated dimensions. The returned cache shares the normal cache storage with dc; the raw dual cache storage remains vector-backed so get_tmp can reinterpret it for the requested automatic differentiation element type.\n\n\n\n\n\n","title":"Base.reshape"},{"category":"method","location":"preallocationtools/#PreallocationTools.dualcache-Tuple","page":"API","text":"dualcache(args...; warn_on_resize::Bool = true, kwargs...)\n\nDeprecated alias for DiffCache(args...; warn_on_resize, kwargs...). Use DiffCache in new code.\n\nArguments\n\nargs: positional arguments accepted by DiffCache.\n\nKeyword Arguments\n\nwarn_on_resize: forwarded to DiffCache; controls whether an undersized dual workspace emits its one-time resize warning.\nkwargs: additional keywords accepted by DiffCache.\n\n\n\n\n\n","title":"PreallocationTools.dualcache"},{"category":"method","location":"preallocationtools/#PreallocationTools.get_tmp-Tuple{FixedSizeDiffCache, Union{Number, AbstractArray}}","page":"API","text":"get_tmp(cache, u)\nget_tmp(cache, u, size)\n\nReturn cache storage appropriate for u.\n\nFor DiffCache and FixedSizeDiffCache, this returns normal storage when u has the cached primal element type and dual-compatible storage when u carries automatic differentiation element types. For LazyBufferCache and GeneralLazyBufferCache, this lazily creates and reuses storage keyed by the type and size requested.\n\nArguments\n\ncache: a cache created by this package, or another object for which the generic fallback should return cache itself.\nu: a value, array, or scalar type describing the element type and, for lazy buffers, the requested shape.\nsize: optional lazy-buffer shape; it is accepted only by LazyBufferCache.\n\nReturn\n\nThe returned workspace is owned by cache and is reused by later matching lookups. Callers must fully overwrite scratch storage before reading it and must not retain it across calls that can request the same cache entry.\n\nExamples\n\njulia> using PreallocationTools\n\njulia> cache = DiffCache(zeros(2), 1);\n\njulia> get_tmp(cache, zeros(2)) === cache.du\ntrue\n\n\n\n\n\n","title":"PreallocationTools.get_tmp"},{"category":"section","location":"#PreallocationTools.jl","page":"Home","text":"PreallocationTools.jl is a set of tools for helping build non-allocating pre-cached functions for high-performance computing in Julia. Its tools handle edge cases of automatic differentiation to make it easier for users to get high performance, even in the cases where code generation may change the function that is being called.","title":"PreallocationTools.jl"},{"category":"section","location":"#DiffCache","page":"Home","text":"DiffCache is a method for generating doubly-preallocated vectors which are compatible with non-allocating forward-mode automatic differentiation by ForwardDiff.jl. Since ForwardDiff uses chunked duals in its forward pass, two vector sizes are required in order for the arrays to be properly defined. DiffCache creates a dispatching type to solve this, so that by passing a qualifier it can automatically switch between the required cache. This method is fully type-stable and non-dynamic, made for when the highest performance is needed.","title":"DiffCache"},{"category":"section","location":"#Using-DiffCache","page":"Home","text":"DiffCache(u::AbstractArray, N::Int = ForwardDiff.pickchunksize(length(u)); levels::Int = 1)\nDiffCache(u::AbstractArray, N::AbstractArray{<:Int})\n\nThe DiffCache function builds a DiffCache object that stores both a version of the cache for u and for the Dual version of u, allowing use of pre-cached vectors with forward-mode automatic differentiation. Note that DiffCache, due to its design, is only compatible with arrays that contain concretely typed elements.\n\nTo access the caches, one uses:\n\nget_tmp(tmp::DiffCache, u)\n\nWhen u has an element subtype of Dual numbers, then it returns the Dual version of the cache. Otherwise, it returns the standard cache (for use in the calls without automatic differentiation).\n\nIn order to preallocate to the right size, the DiffCache needs to be specified to have the correct N matching the chunk size of the dual numbers or larger. If the chunk size N specified is too large, get_tmp will automatically resize when dispatching; this remains type-stable and non-allocating, but comes at the expense of additional memory.\n\nIn a differential equation, optimization, etc., the default chunk size is computed from the state vector u, and thus if one creates the DiffCache via DiffCache(u) it will match the default chunking of the solver libraries.\n\nDiffCache is also compatible with nested automatic differentiation calls through the levels keyword (N for each level computed using based on the size of the state vector) or by specifying N as an array of integers of chunk sizes, which enables full control of chunk sizes on all differentiation levels.\n\nFor resizeable multidimensional workspaces, keep the backing DiffCache one-dimensional and reshape it when a multidimensional cache is needed:\n\nstorage = DiffCache(zeros(10), 2)\ncache = reshape(storage, 2, 5)\n\nresize!(storage, 12)\ncache = reshape(storage, 2, 6)\n\nThe reshaped cache shares the normal cache storage with storage, while the dual cache storage remains vector-backed for get_tmp.","title":"Using DiffCache"},{"category":"section","location":"#DiffCache-Example-1:-Direct-Usage","page":"Home","text":"using ForwardDiff, PreallocationTools\nrandmat = rand(5, 3)\nsto = similar(randmat)\nstod = DiffCache(sto)\n\nfunction claytonsample!(sto, τ, α; randmat = randmat)\n    sto = get_tmp(sto, τ)\n    sto .= randmat\n    τ == 0 && return sto\n\n    n = size(sto, 1)\n    for i in 1:n\n        v = sto[i, 2]\n        u = sto[i, 1]\n        sto[i, 1] = (1 - u^(-τ) + u^(-τ) * v^(-(τ / (1 + τ))))^(-1 / τ) * α\n        sto[i, 2] = (1 - u^(-τ) + u^(-τ) * v^(-(τ / (1 + τ))))^(-1 / τ)\n    end\n    return sto\nend\n\nForwardDiff.derivative(τ -> claytonsample!(stod, τ, 0.0), 0.3)\nForwardDiff.jacobian(x -> claytonsample!(stod, x[1], x[2]), [0.3; 0.0])\n\nIn the above, the chunk size of the dual numbers has been selected based on the size of randmat, resulting in a chunk size of 8 in this case. However, since the derivative is calculated with respect to τ and the Jacobian is calculated with respect to τ and α, specifying the DiffCache with stod = DiffCache(sto, 1) or stod = DiffCache(sto, 2), respectively, would have been the most memory efficient way of performing these calculations (only really relevant for much larger problems).","title":"DiffCache Example 1: Direct Usage"},{"category":"section","location":"#DiffCache-Example-2:-ODEs","page":"Home","text":"using LinearAlgebra, OrdinaryDiffEq\nfunction foo(du, u, (A, tmp), t)\n    mul!(tmp, A, u)\n    @. du = u + tmp\n    nothing\nend\nprob = ODEProblem(foo, ones(5, 5), (0.0, 1.0), (ones(5, 5), zeros(5, 5)))\nsolve(prob, TRBDF2())\n\nfails because tmp is only real numbers, but during automatic differentiation we need tmp to be a cache of dual numbers. Since u is the value that will have the dual numbers, we dispatch based on that:\n\nusing LinearAlgebra, OrdinaryDiffEq, PreallocationTools\nfunction foo(du, u, (A, tmp), t)\n    tmp = get_tmp(tmp, u)\n    mul!(tmp, A, u)\n    @. du = u + tmp\n    nothing\nend\nchunk_size = 5\nprob = ODEProblem(foo,\n    ones(5, 5),\n    (0.0, 1.0),\n    (ones(5, 5), DiffCache(zeros(5, 5), chunk_size)))\nsolve(prob, TRBDF2(chunk_size = chunk_size))\n\nor just using the default chunking:\n\nusing LinearAlgebra, OrdinaryDiffEq, PreallocationTools\nfunction foo(du, u, (A, tmp), t)\n    tmp = get_tmp(tmp, u)\n    mul!(tmp, A, u)\n    @. du = u + tmp\n    nothing\nend\nchunk_size = 5\nprob = ODEProblem(foo, ones(5, 5), (0.0, 1.0), (ones(5, 5), DiffCache(zeros(5, 5))))\nsolve(prob, TRBDF2())","title":"DiffCache Example 2: ODEs"},{"category":"section","location":"#DiffCache-Example-3:-Nested-AD-calls-in-an-optimization-problem-involving-a-Hessian-matrix","page":"Home","text":"using LinearAlgebra, OrdinaryDiffEq, PreallocationTools, Optim, Optimization\nfunction foo(du, u, p, t)\n    tmp = p[2]\n    A = reshape(p[1], size(tmp.du))\n    tmp = get_tmp(tmp, u)\n    mul!(tmp, A, u)\n    @. du = u + tmp\n    nothing\nend\n\ncoeffs = -collect(0.1:0.1:0.4)\ncache = DiffCache(zeros(2, 2), levels = 3)\nprob = ODEProblem(foo, ones(2, 2), (0.0, 1.0), (coeffs, cache))\nrealsol = solve(prob, TRBDF2(), saveat = 0.0:0.1:10.0, reltol = 1e-8)\n\nfunction objfun(x, prob, realsol, cache)\n    prob = remake(prob, u0 = eltype(x).(prob.u0), p = (x, cache))\n    sol = solve(prob, TRBDF2(), saveat = 0.0:0.1:10.0, reltol = 1e-8)\n\n    ofv = 0.0\n    if any((s.retcode != :Success for s in sol))\n        ofv = 1e12\n    else\n        ofv = sum((sol .- realsol) .^ 2)\n    end\n    return ofv\nend\nfn(x, p) = objfun(x, p[1], p[2], p[3])\noptfun = OptimizationFunction(fn, Optimization.AutoForwardDiff())\noptprob = OptimizationProblem(optfun, zeros(length(coeffs)), (prob, realsol, cache))\nsolve(optprob, Newton())\n\nSolves an optimization problem for the coefficients, coeffs, appearing in a differential equation. The optimization is done with Optim.jl's Newton() algorithm. Since this involves automatic differentiation in the ODE solver and the calculation of Hessians, three automatic differentiations are nested within each other. Therefore, the DiffCache is specified with levels = 3.","title":"DiffCache Example 3: Nested AD calls in an optimization problem involving a Hessian matrix"},{"category":"section","location":"#FixedSizeDiffCache","page":"Home","text":"FixedSizeDiffCache is a lot like DiffCache, but it stores dual numbers in its caches instead of a flat array. Because of this, it can avoid a view, making it a little more performant for generating caches of non-Array types. However, it is a lot less flexible than DiffCache, and is thus only recommended for cases where the chunk size is known in advance (for example, ODE solvers) and where u is not an Array.\n\nThe interface is almost exactly the same, except with the constructor:\n\nFixedSizeDiffCache(u::AbstractArray, chunk_size = Val{ForwardDiff.pickchunksize(length(u))})\nFixedSizeDiffCache(u::AbstractArray, chunk_size::Integer)\n\nNote that the FixedSizeDiffCache can support duals that are of a small chunk size than the preallocated ones, but not a larger size. Nested duals are not supported with this construct.","title":"FixedSizeDiffCache"},{"category":"section","location":"#LazyBufferCache","page":"Home","text":"LazyBufferCache(f::F = identity)\n\nA LazyBufferCache is a Dict-like type for the caches, which automatically defines new cache arrays on demand when they are required. The function f maps size_of_cache = f(size(u)), which by default creates cache arrays of the same size.\n\nBy default the created buffers are not initialized, but a function initializer! can be supplied which is applied to the buffer when it is created, for instance buf -> fill!(buf, 0.0).\n\nNote that LazyBufferCache is type-stable and contains no dynamic dispatch. This gives it a ~15ns overhead. The upside of LazyBufferCache is that the user does not have to worry about potential issues with chunk sizes and such: LazyBufferCache is much easier!","title":"LazyBufferCache"},{"category":"section","location":"#Example","page":"Home","text":"using LinearAlgebra, OrdinaryDiffEq, PreallocationTools\nfunction foo(du, u, (A, lbc), t)\n    tmp = lbc[u]\n    mul!(tmp, A, u)\n    @. du = u + tmp\n    nothing\nend\nprob = ODEProblem(foo, ones(5, 5), (0.0, 1.0), (ones(5, 5), LazyBufferCache()))\nsolve(prob, TRBDF2())","title":"Example"},{"category":"section","location":"#Note-About-ReverseDiff-Support-for-LazyBuffer","page":"Home","text":"ReverseDiff support is done in SciMLSensitivity.jl to reduce the AD requirements on this package. Load that package if ReverseDiff overloads are required.","title":"Note About ReverseDiff Support for LazyBuffer"},{"category":"section","location":"#GeneralLazyBufferCache","page":"Home","text":"GeneralLazyBufferCache(f = identity)\n\nA GeneralLazyBufferCache is a Dict-like type for the caches, which automatically defines new caches on demand when they are required. The function f generates the cache matching for the type of u, and subsequent indexing reuses that cache if that type of u has already been seen.\n\nNote that GeneralLazyBufferCache's return is not type-inferred. This means it's the slowest of the preallocation methods, but it's the most general.","title":"GeneralLazyBufferCache"},{"category":"section","location":"#Example-2","page":"Home","text":"In all the previous cases, our cache was an array. However, in this case, we want to preallocate a DifferentialEquations ODEIntegrator object. This object is the one created via DifferentialEquations.init(ODEProblem(ode_fnc, y₀, (0.0, T), p), Tsit5(); saveat = t), and we want to optimize p in a way that changes its type to ForwardDiff. Thus, what we can do is make a GeneralLazyBufferCache which holds these integrator objects, defined by p, and indexing it with p in order to retrieve the cache. The first time it's called it will build the integrator, and in subsequent calls it will reuse the cache.\n\nDefining the cache as a function of p to build an integrator thus looks like:\n\nlbc = GeneralLazyBufferCache(function (p)\n    DifferentialEquations.init(ODEProblem(ode_fnc, y₀, (0.0, T), p), Tsit5(); saveat = t)\nend)\n\nthen lbc[p] will be smart and reuse the caches. A full example looks like the following:\n\nusing Random, DifferentialEquations, LinearAlgebra, Optimization, OptimizationNLopt,\n      OptimizationOptimJL, PreallocationTools\n\nlbc = GeneralLazyBufferCache(function (p)\n    DifferentialEquations.init(ODEProblem(ode_fnc, y₀, (0.0, T), p), Tsit5(); saveat = t)\nend)\n\nRandom.seed!(2992999)\nλ, y₀, σ = -0.5, 15.0, 0.1\nT, n = 5.0, 200\nΔt = T / n\nt = [j * Δt for j in 0:n]\ny = y₀ * exp.(λ * t)\nyᵒ = y .+ [0.0, σ * randn(n)...]\node_fnc(u, p, t) = p * u\nfunction loglik(θ, data, integrator)\n    yᵒ, n, ε = data\n    λ, σ, u0 = θ\n    integrator.p = λ\n    reinit!(integrator, u0)\n    solve!(integrator)\n    ε = yᵒ .- integrator.sol.u\n    ℓ = -0.5n * log(2π * σ^2) - 0.5 / σ^2 * sum(ε .^ 2)\nend\nθ₀ = [-1.0, 0.5, 19.73]\nnegloglik = (θ, p) -> -loglik(θ, p, lbc[θ[1]])\nfnc = OptimizationFunction(negloglik, Optimization.AutoForwardDiff())\nε = zeros(n)\nprob = OptimizationProblem(fnc,\n    θ₀,\n    (yᵒ, n, ε),\n    lb = [-10.0, 1e-6, 0.5],\n    ub = [10.0, 10.0, 25.0])\nsolve(prob, LBFGS())","title":"Example"},{"category":"section","location":"#Enzyme-Support","page":"Home","text":"Code written against the DiffCache/get_tmp API for ForwardDiff compatibility can also be differentiated with Enzyme.jl, in both forward and reverse mode. Enzyme itself does not need the dual caches — custom EnzymeRules are provided that hand Enzyme correctly-shaped shadow buffers so that the same cached code works with either AD engine:\n\nusing Enzyme, PreallocationTools\n\nrandmat = rand(5, 3)\ncache = DiffCache(similar(randmat))\n\nfunction f(τ, cache)\n    tmp = get_tmp(cache, τ)\n    @. tmp = randmat * τ^2\n    return sum(abs2, tmp)\nend\n\n# Forward mode\nEnzyme.autodiff(Forward, f, Duplicated(0.3, 1.0), Const(cache))\n\n# Reverse mode\nEnzyme.autodiff(Reverse, f, Active, Active(0.3), Const(cache))\n\nThe cache can be passed to Enzyme as Const (as above, including implicitly by enclosing it in a Const closure or parameter struct) or as Duplicated(cache, Enzyme.make_zero(cache)). When the cache is Const, a hidden shadow cache is maintained per primal cache, kept separate from the ForwardDiff dual buffer so that Enzyme can even differentiate code that calls ForwardDiff internally (Enzyme-over-ForwardDiff). DiffCache, FixedSizeDiffCache, and LazyBufferCache are supported; GeneralLazyBufferCache is not, since its buffers are built by an arbitrary user function.\n\nThese rules are correct under the documented usage contract of the caches: the buffer returned by get_tmp is scratch space that is fully written within the function that fetches it before being read, and its contents are not relied upon to persist across separate calls into the differentiated code.","title":"Enzyme Support"},{"category":"section","location":"#Second-order-differentiation-with-Enzyme","page":"Home","text":"The following nested differentiation patterns are supported and tested (set_runtime_activity is required by Enzyme for some of them, as it is for the equivalent nesting of cache-free code):\n\nForward-mode Enzyme over ForwardDiff\nReverse-mode Enzyme over ForwardDiff\nForward-mode Enzyme over forward-mode Enzyme\nForward-mode Enzyme over reverse-mode Enzyme\nReverse-mode Enzyme over forward-mode Enzyme\n\nThe Enzyme-over-Enzyme combinations currently require Julia < 1.12: on 1.12+ Enzyme fails to compile nested differentiation through these rules (EnzymeAD/Enzyme.jl#3315). Enzyme over ForwardDiff works on all supported Julia versions.","title":"Second-order differentiation with Enzyme"},{"category":"section","location":"#Similar-Projects","page":"Home","text":"AutoPreallocation.jl tries to do this automatically at the compiler level. Alloc.jl tries to do this with a bump allocator.","title":"Similar Projects"},{"category":"section","location":"#Contributing","page":"Home","text":"Please refer to the SciML ColPrac: Contributor's Guide on Collaborative Practices for Community Packages for guidance on PRs, issues, and other matters relating to contributing to SciML.\nSee the SciML Style Guide for common coding practices and other style decisions.\nThere are a few community forums:\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Slack\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Zulip\nOn the Julia Discourse forums\nSee also SciML Community page","title":"Contributing"},{"category":"section","location":"#Reproducibility","page":"Home","text":"<details><summary>The documentation of this SciML package was built using these direct dependencies,</summary>\n\nusing Pkg # hide\nPkg.status() # hide\n\n</details>\n\n<details><summary>and using this machine and Julia version.</summary>\n\nusing InteractiveUtils # hide\nversioninfo() # hide\n\n</details>\n\n<details><summary>A more complete overview of all dependencies and their versions is also provided.</summary>\n\nusing Pkg # hide\nPkg.status(; mode = PKGMODE_MANIFEST) # hide\n\n</details>\n\nusing TOML\nusing Markdown\nversion = TOML.parse(read(\"../../Project.toml\", String))[\"version\"]\nname = TOML.parse(read(\"../../Project.toml\", String))[\"name\"]\nlink_manifest = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n                \"/assets/Manifest.toml\"\nlink_project = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n               \"/assets/Project.toml\"\nMarkdown.parse(\"\"\"You can also download the\n[manifest]($link_manifest)\nfile and the\n[project]($link_project)\nfile.\n\"\"\")","title":"Reproducibility"},{"category":"section","location":"internals/#Internal-Functions","page":"Internals","text":"This page covers implementation details that are not part of the supported API.\n\nwarning: Warning\n\n\nThese implementation details may change without notice and should not be used by user code. Versioned extension points are documented separately under Developer API.","title":"Internal Functions"}]
}
